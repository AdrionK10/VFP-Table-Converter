*
OPEN DATA ?

IF NOT USED("SysGenPK")
 USE SysGenPK AGAIN IN 0 ALIAS SysGenPK
ENDIF

IF NOT USED("INITM")
 USE INITM AGAIN IN 0 ALIAS INITM
ENDIF

IF NOT USED("INITM2")
 USE INITM2 AGAIN IN 0 ALIAS INITM2
ENDIF

IF NOT USED("INLOC")
 USE INLOC AGAIN IN 0 ALIAS INLOC
ENDIF
SET ORDER TO CODE IN INLOC

IF NOT USED("INITMLOC")
 USE INITMLOC AGAIN IN 0 ALIAS INITMLOC
ENDIF

IF NOT USED("INPRD")
 USE INPRD AGAIN IN 0 ALIAS INPRD
ENDIF

IF NOT USED("INITMC01")
 USE INITMC01 AGAIN IN 0 ALIAS INITMC01
ENDIF

IF NOT USED("INITMC02")
 USE INITMC02 AGAIN IN 0 ALIAS INITMC02
ENDIF

IF NOT USED("INITMC03")
 USE INITMC03 AGAIN IN 0 ALIAS INITMC03
ENDIF

IF NOT USED("INPRDC01")
 USE INPRDC01 AGAIN IN 0 ALIAS INPRDC01
ENDIF

IF NOT USED("INPRDC02")
 USE INPRDC02 AGAIN IN 0 ALIAS INPRDC02
ENDIF

IF NOT USED("INPRDC03")
 USE INPRDC03 AGAIN IN 0 ALIAS INPRDC03
ENDIF

IF NOT USED("INTRN")
 USE INTRN AGAIN IN 0 ALIAS INTRN
ENDIF
